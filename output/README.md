# Output Directory Guide

Everything in this folder is generated. Nothing here is edited by hand, and
every file has a named generator in `code/`. All of it rebuilds from the
committed data:

```bash
python code/00_run_all.py --with-present
```

If a file here ever disagrees with the manuscript, the generated file is
correct and the manuscript needs fixing. That rule exists because an earlier
submission carried two numbers that had been transcribed by hand from a
narrative document no script produced. That document has been removed.

---

## Start here if you are reviewing the analysis

### `validation/` — the paper checked against the data

| File | What it is |
|---|---|
| `manuscript_check.txt` | Every value in the manuscript compared with the data, cell by cell. Current result: **123 checks, 0 mismatches** |
| `manuscript_numbers.txt` | Every number the paper quotes, recomputed and formatted for reading |
| `manuscript_numbers.json` | The same values at full precision, plus model diagnostics |

Generated by `code/12_manuscript_numbers.py`. The JSON also carries a
`model_diagnostics` block with outcome overdispersion, negative binomial versus
Poisson AIC for all eight models, and sensitivity to fixing the dispersion
parameter. See `docs/REPRODUCIBILITY.md` for how to read it.

### `results/` — model results, machine-readable

| File | What it is |
|---|---|
| `index_comparison_results.json` | Every model estimate at full precision: the SVI, the purpose-built EDI, and the external Graham Center SDI, each unadjusted and adjusted for metropolitan status. Also the SVI–EDI and SDI–EDI correlations and each index's association with rurality |
| `index_comparison_results.txt` | The same results formatted for reading |

Generated by `code/09_validated_index_sdi.py`. **Figures 1B and S and the
Table 4 Word files are all drawn from the JSON**, so a plotted value and a
reported value cannot drift apart.

> **Read estimates from the JSON, not the `.txt`.** The text file prints three
> decimal places for readability. Rounding a 3-dp value again to 2 dp gives the
> wrong answer for some estimates — the SVI-adjusted CMR lower bound is
> 0.954948, which is 0.95, but reads as 0.955 in the text file and rounds a
> second time to 0.96. The manuscript and figures round once, from full
> precision.

---

## The rest

### `figures/`

| File | Manuscript role |
|---|---|
| `figure1_choropleth.png` | Figure 1A. County choropleth of CMR and CCT capacity |
| `Figure1B_Unadjusted_vs_Adjusted.pdf` / `.png` | Figure 1B. SVI and EDI, each unadjusted and adjusted, with metropolitan status in its own panel |
| `Figure2_SVI_vs_EDI_Comparison.pdf` / `.png` | Figure 2. The two indices side by side |
| `Figure3_EDI_Quintile_Rates.pdf` / `.png` | Figure 3. Capacity by EDI quintile |
| `FigureS_SDI_External_Validation.pdf` / `.png` | Figure S. The EDI against the external Graham Center SDI |

From `04_choropleth_map.py`, `08_svi_edi_comparison_maps.py`,
`10_forest_plots.py`, and `07_publication_outputs.py`.

### `tables/`

| File | Manuscript role |
|---|---|
| `Table4_External_Validation_SDI.docx` | Table 4 |
| `Table4_SVI_vs_EDI_Comparison.docx` | Index comparison |
| `Supplementary_Table_EDI_Regression.docx` | Supplementary EDI models, including the stratified and ordinal-RUCC variants |
| `additional_statistics.json` | Descriptive statistics, with every "N (P%)" pair asserted internally consistent at run time |
| `table1_publication.*`, `table1_descriptive.csv`, `table1_formatted.txt` | Table 1 in several renderings |

From `03_descriptive_analysis.py`, `11_edi_tables_and_stats.py`, and
`07_publication_outputs.py`.

**Manuscript Tables 1, 2 and 3 live in the manuscript file itself**, where
`12_manuscript_numbers.py` checks them cell by cell. Standalone `.docx` copies
of them used to sit here; they were removed because no script generated them
and they had gone stale, still carrying the retired "ADI" label and
pre-adjustment estimates.

### `models/`

| File | What it is |
|---|---|
| `regression_results.txt` | SVI negative binomial models |
| `regression_results_full.txt` | Full model output including diagnostics |
| `EDI_Regression_Results.txt` | EDI models: unadjusted, metro-adjusted, ordinal-RUCC, and metro-stratified |

From `05_regression_analysis.py`, `06_edi_sensitivity_analysis.py`, and
`07_publication_outputs.py`. `model_objects.pkl` is written here too but is
gitignored (size).
