# Analysis pipeline dependencies
# Python 3.11.7
#
#   python3.11 -m venv .venv
#   .venv/bin/python -m pip install -r requirements.txt
#
# Versions are pinned to the environment that produced every committed output.
# requirements-lock.txt additionally pins the transitive dependencies.
#
# Everything needed to reproduce the published numbers is in the "core" block.
# The later blocks are only required for rebuilding figures and documents.

# --- core: data handling and modelling (stages B, C, D) ---
pandas==3.0.5
numpy==1.26.4
# NumPy is capped below 2.0 deliberately. Several compiled wheels in this stack
# (matplotlib, geopandas) are built against the 1.x ABI, and installing NumPy 2.x
# breaks them with '_ARRAY_API not found'. Verified working: numpy 1.26.4.
scipy==1.13.0
statsmodels==0.14.6
scikit-learn==1.4.2          # PCA and standardisation used to construct the EDI
openpyxl==3.1.5              # reads the ACR facility spreadsheets

# --- geospatial: maps and the county geometry file (stages B, E) ---
geopandas==1.1.4
mapclassify==2.10.0           # choropleth classification schemes
pyproj==3.7.2
shapely==2.1.2

# --- figures (stage E) ---
matplotlib==3.7.1

# --- documents: Word tables, slides, and manuscript validation ---
python-docx==1.2.0           # imported as `docx`
python-pptx==1.0.2           # imported as `pptx`
lxml==6.1.1                  # reads tracked changes when validating the manuscript

# --- optional: one workflow PDF in 07_publication_outputs.py ---
# WeasyPrint also needs system libraries (pango, cairo). The pipeline skips this
# single document gracefully if it is absent; no published number depends on it.
# weasyprint>=60

# --- data acquisition (stage A, network required) ---
requests==2.34.2
